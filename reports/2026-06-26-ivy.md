---
title: 渔场养鱼游戏 P1 实装 · 发现模块（渔场/钓点/活动）前后端
author: ivy
date: 2026-06-26
tags: [mobile, backend, fishery, game, discovery]
---

> 今日提交：5 个（移动端 3 + 后端 2）
> 今日重点：渔场养鱼游戏 P1 前后端实装、地图发现模块（渔场/钓点/活动）前后端打通

## 一、渔场养鱼游戏 P1 实装（前后端）

把渔场养鱼游戏从原型/规划推进到 P1 实装，养鱼核心闭环成形。

**后端**
- 新增 fishery 模块：鱼种模板、鱼实例、饲料类型、库存等数据模型与迁移（含索引）
- fishery 路由支持列表、入缸（stock）、放生（release）、投喂（feed）
- Store 接口与 PostgresStore 实现对应养鱼操作

**移动端**
- 新增 `fishery_game_models.dart`（FishSpecies/FishInstance/FeedType/ShopItem 等）、`fishery_config.dart` 种子配置、`fishery_game_store.dart` 状态管理
- 重写 `FisheryTabScreen`：接入养鱼游戏逻辑与 UI（数据条 + 场景 + 操作）
- 新增 `manage_fish_modal.dart` 管理鱼的弹窗（买/卖、培育等交互）
- `CatchRecord` 增加 `speciesId`，打通「渔获 → 入缸」识别衔接
- 落地 P1 实施计划文档（约 2000 行，架构/技术栈/全局约束）

## 二、地图发现模块（渔场/钓点/活动）前后端

为地图 tab 的「附近发现」补上真实数据来源，替换此前的 mock。

**后端**
- 新增 discovery 模块：渔场（fisheries）、钓点（fishing spots）、用户发起的活动（activities）
- 路由支持公开/受保护两类：列表、详情、创建活动、加入活动（含活动满员错误处理）
- 配套数据模型、迁移（索引）、Store 实现，并加 seed 种子数据

**移动端**
- 新增 `discovery_repository.dart`：对接附近渔场、排行、活动的 API
- 实现创建/加入活动方法（含活动已满的错误处理）
- 新增对应响应数据模型，地图标记改用居中对齐

## 三、Godot 集成收尾（移动端）

- 延续 06-25 的独立进程方案，`47986aa` 继续重构 Android 端 Godot 集成（`RemoteGodotFragment` 相关收尾）

---

**总结**：今天是渔场与地图发现两条线同时落地——渔场养鱼游戏 P1 前后端实装（鱼种/实例/饲料/库存模型 + 入缸/放生/投喂接口 + 客户端 store/UI/管理弹窗，并打通渔获入缸），地图「附近发现」从 mock 升级为真实数据（后端 discovery 模块覆盖渔场/钓点/活动含活动报名，前端 discovery_repository 对接）。这两天整体看，是把游戏渲染（Godot 集成）和游戏/发现的业务数据层都补齐了。

## 提交明细

### fsp-mobile-app

| 时间 | 提交 | 说明 |
|------|------|------|
| 14:17 | 34645a2 | Add discovery repository and endpoints for fisheries and activities |
| 14:05 | ee23fa6 | Update marker alignment in MapTabScreen to use center alignment |
| 14:04 | 026711b | Add fishery game implementation and configuration |
| 09:08 | 47986aa | Refactor Godot integration in Android app |

### fsp-backend

| 时间 | 提交 | 说明 |
|------|------|------|
| 14:01 | ee9d53d | Add discovery module with fisheries, fishing spots, and activities |
| 12:18 | 20b2572 | Add fishery functionality with species and fish management |
